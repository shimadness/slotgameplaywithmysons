{
  "name": "triple-slot",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "TRIPLE SLOT — an original browser slot game (puzzle x slot), built to play with my sons.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "rtp": "npx tsx rtp.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.8"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}
